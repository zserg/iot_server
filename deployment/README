# Vagrant VM
ansible-playbook -i hosts --extra-vars="target=debian_vm" -s initialize_basic_user.yml
ansible-playbook -i hosts --extra-vars="target=debian_vm DBUSER=iot_server" --tags="initial" provision.yml
# Uncomment the following line in Vagrantfile
#config.vm.synced_folder "../", "/var/projects/iot_server/code", :owner => "iot_server", :group => "iot_server"
vagrant reload

ansible-playbook -i hosts --extra-vars="target=debian_vm DBUSER=iot_server"  provision.yml
#---------------------------------------




ansible-playbook -i hosts --extra-vars="target=vps2" -u root -k initialize_basic_user.yml

